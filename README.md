## Como rodar o projeto:

```bash
clone o projeto, abra o terminal e digite: npm run dev

Abra [http://localhost:3000](http://localhost:3000) no seu navegador.

## Você pode inserir qualquer usuario do github no login vai aparecer a foto dele e ele vai poder se logar no chat

O backend foi feito com o *supabase*
